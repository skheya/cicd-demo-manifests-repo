apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-app:55
  labels:
    app: nginx
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: todo
        image: mehedi4475/todo-app:55v1
        ports:
        - containerPort: 8000
